---
import type { Shop } from '../data/shops';
import type { RestaurantTheme } from '../themes/restaurantThemes';

interface Props {
  shop: Shop;
  theme: RestaurantTheme;
}
const { shop, theme } = Astro.props;
---

<section class="mx-auto max-w-content px-5 py-12 sm:py-16">
  <p class="text-xs tracking-[0.3em] text-accent">ABOUT</p>
  <h2 class="mt-2 font-heading text-2xl text-ink sm:text-3xl" style="letter-spacing:var(--tracking)">
    {shop.name}について
  </h2>
  <div class="mt-8 grid gap-6 sm:grid-cols-3 sm:gap-8">
    {
      shop.about.map((a, i) => (
        <div class="border-t border-line pt-5">
          <span class="font-heading text-sm text-accent">0{i + 1}</span>
          <h3 class="mt-2 font-heading text-lg text-ink">{a.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-muted">{a.body}</p>
        </div>
      ))
    }
  </div>
</section>
