/**
 * Future implementation note:
 * This script will read the generated site-spec markdown and call Claude API
 * to produce Astro + Tailwind project files.
 *
 * Planned responsibilities:
 * 1) Load spec markdown from data/outputs/*.md
 * 2) Build prompt using prompts/claude-generate-site.md
 * 3) Request code generation from Claude
 * 4) Write files into generated site directory
 *
 * Not implemented in MVP.
 */
